[general]
dirs = ["src/_templates"]
whitespace = "preserve"
default_syntax = "custom"

[[syntax]]
name = "custom"

block_start = "{{"
block_end = "}}"

comment_start = "{#"
comment_end = "#}"

expr_start = "{:"
expr_end = ":}"
